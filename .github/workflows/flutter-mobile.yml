name: Flutter Mobile App CI

on:
  push:
    branches: [ main, develop ]
    paths:
      - 'ruche-connectee/mobile-app/**'
  pull_request:
    branches: [ main ]
    paths:
      - 'ruche-connectee/mobile-app/**'

defaults:
  run:
    working-directory: ruche-connectee/mobile-app

jobs:
  # Validation du code Flutter (focus web)
  test:
    name: Flutter Code Quality
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.19.x'
        cache: true

    - name: Install dependencies
      run: flutter pub get

    - name: Verify the install
      run: flutter doctor -v

    - name: Check formatting
      run: dart format --output=none --set-exit-if-changed .

    - name: Analyze project source
      run: flutter analyze --fatal-infos

    # Tests désactivés - focus sur web seulement
    # - name: Run tests
    #   run: flutter test --coverage

    # - name: Upload coverage reports
    #   uses: codecov/codecov-action@v4
    #   with:
    #     files: ruche-connectee/mobile-app/coverage/lcov.info
    #     flags: flutter
    #     name: flutter-coverage

  # Build Android désactivé - focus sur web
  # build-android:
  #   name: Build Android APK
  #   runs-on: ubuntu-latest
  #   needs: test

  #   steps:
  #   - name: Checkout code
  #     uses: actions/checkout@v4

  #   - name: Setup Java
  #     uses: actions/setup-java@v4
  #     with:
  #       distribution: 'temurin'
  #       java-version: '17'

  #   - name: Setup Flutter
  #     uses: subosito/flutter-action@v2
  #     with:
  #       flutter-version: '3.19.x'
  #       cache: true

  #   - name: Install dependencies
  #     run: flutter pub get

  #   - name: Build APK
  #     run: flutter build apk --release

  #   - name: Upload APK artifact
  #     uses: actions/upload-artifact@v4
  #     with:
  #       name: release-apk
  #       path: ruche-connectee/mobile-app/build/app/outputs/flutter-apk/app-release.apk

  # Build iOS désactivé - focus sur web
  # build-ios:
  #   name: Build iOS App
  #   runs-on: macos-latest
  #   needs: test
  #   if: github.event_name == 'push' && github.ref == 'refs/heads/main'

  #   steps:
  #   - name: Checkout code
  #     uses: actions/checkout@v4

  #   - name: Setup Flutter
  #     uses: subosito/flutter-action@v2
  #     with:
  #       flutter-version: '3.19.x'
  #       cache: true

  #   - name: Install dependencies
  #     run: flutter pub get

  #   - name: Build iOS (no codesign)
  #     run: flutter build ios --release --no-codesign

  #   - name: Create iOS archive
  #     run: |
  #       cd ios
  #       xcodebuild -workspace Runner.xcworkspace \
  #                  -scheme Runner \
  #                  -sdk iphoneos \
  #                  -configuration Release \
  #                  -archivePath build/Runner.xcarchive \
  #                  clean archive \
  #                  CODE_SIGNING_ALLOWED=NO

  # Build pour Web - Focus principal
  build-web:
    name: Build Web App
    runs-on: ubuntu-latest
    needs: test

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.19.x'
        cache: true

    - name: Install dependencies
      run: flutter pub get

    - name: Build Web
      run: flutter build web --release

    - name: Upload Web build
      uses: actions/upload-artifact@v4
      with:
        name: web-build
        path: ruche-connectee/mobile-app/build/web/

  # Analyse de sécurité
  security:
    name: Security Analysis
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.19.x'
        cache: true

    - name: Install dependencies
      run: flutter pub get

    - name: Check for vulnerable packages
      run: flutter pub deps --style=compact

    - name: Run security audit
      run: dart pub audit
      continue-on-error: true

    - name: Check for outdated packages
      run: flutter pub outdated
      continue-on-error: true