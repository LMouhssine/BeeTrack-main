# Configuration Docker pour BeeTrack
# Copiez ce fichier vers .env et modifiez les valeurs selon vos besoins

# Base de données Redis
REDIS_PASSWORD=beetrackredis123

# Monitoring (optionnel)
GRAFANA_PASSWORD=admin123

# Application Spring Boot
SPRING_PROFILES_ACTIVE=docker
JAVA_OPTS=-Xmx1024m -Xms512m

# Firebase Configuration (à configurer dans l'application)
# Ces variables peuvent être utilisées pour configurer Firebase
FIREBASE_PROJECT_ID=your-firebase-project-id
FIREBASE_DATABASE_URL=https://your-project-default-rtdb.firebaseio.com/

# Logs
LOG_LEVEL=INFO

# SSL/TLS (pour production)
SSL_CERT_PATH=/etc/nginx/ssl/cert.pem
SSL_KEY_PATH=/etc/nginx/ssl/key.pem

# Domaines autorisés (CORS)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080

# Sécurité
JWT_SECRET=your-jwt-secret-key-here
ENCRYPTION_KEY=your-encryption-key-here